# Git
.git
.gitignore
.gitmodules

# Dependencies
node_modules
**/node_modules
.venv
**/.venv
venv
**/venv
__pycache__
**/__pycache__
*.py[cod]
*.egg-info
**/*.egg-info
.pytest_cache
**/.pytest_cache
.notebooklm-cli-venv/
.notebooklm-playwright/
.pip-cache/
.uv-cache/

# Built artifacts that are regenerated inside the image.  Excluded so local
# rebuilds on the developer's machine don't invalidate the npm-install layer
# that now depends on the full ui-tui/packages/intellect-ink/ tree being present.
ui-tui/dist/
ui-tui/packages/intellect-ink/dist/

# CI/CD
.github

# Environment files
.env
.env.*

*.md

# Runtime data (bind-mounted at /opt/data; must not leak into build context)
data/
.intellect-docker/
.notebooklm-home/

# Compose/profile runtime state (bind-mounted; avoid ownership/secret issues)
intellect-config/
runtime/
workspace/

# Rust 本地编译产物和源码运行占位包会在 Linux 镜像中重新生成，不能复制宿主机版本。
target/
**/target/
intellect_community_core/
