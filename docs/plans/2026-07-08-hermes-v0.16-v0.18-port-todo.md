# Hermes v0.16–v0.18 功能移植 — 正式 TODO 任务分解

> 文档日期：2026-07-08
> 状态：🔄 执行中（Phase 0 起）
> 上游文档：`2026-07-08-hermes-v0.16-v0.18-port-design.md`（设计方案 + 评审记录 §九）
> 任务编号：`HP-<阶段><序号>`（Hermes Port）
> 状态标记：⬜ 未开始 | 🔄 进行中 | ✅ 完成 | ⏸️ 暂停 | ❌ 取消

**通用验收标准（适用于所有任务，不再逐条重复）**：
- 全量 `scripts/run_tests.sh` 通过；涉及 Rust 的任务另跑 `cargo test`；
- 新增 slash 命令走 `intellect_cli/commands/registry.py` 中央注册表；
- 新增配置键覆盖三条 config loader（`load_cli_config` / `load_config` / gateway 直读）；
- 新增持久化 schema 在 Rust 层落地，Python 仅薄封装；
- 不引入无上界依赖；不破坏 prompt caching。

---

## Phase 0 — 例行安全轮（立即执行，不阻塞评审）

### [HP-001] ✅ v0.16–v0.18 窗口安全修复逐 commit 核对移植

**设计依据**: §5.12 | **审计**: `2026-07-08-hermes-port-phase0-security-audit.md`
**本 Phase 已移植**:
- [x] Slack `xapp-` token 脱敏 → `agent/redact.py`, `gateway/helpers.py`
- [x] MCP auth env key 消毒 → `intellect_cli/mcp_config.py`
- [x] aiohttp `client_max_size` → bluebubbles, teams, proxy
**已有等价 / 无需改动**:
- [x] 浏览器 cloud-metadata floor（`sandbox.rs`, `browser_tool.py`）
- [x] aiohttp 版本 pin（`pyproject.toml`）
**暂缓**:
- [ ] MCP 整表 replace 持久化
- [ ] cron profile secret scope（无 secret scope API）

### [HP-002] ✅ 文档陈旧修正（评审顺手项）

- [x] AGENTS.md：命令注册表路径 → `intellect_cli/commands/registry.py`
- [x] `docs/architecture/rust-python-interaction.md` §2：Rust/Python 0.6.7 对齐

---

## Phase 1 — P0 低风险项（互相独立，可并行）

> **细化与评审**：`2026-07-08-hermes-phase1-refinement-review.md`（2026-07-08）
> **推荐顺序**：HP-101 ∥ HP-102 → HP-103-MVP（OpenAI）；FAL 可溢出 Phase 2

### [HP-101] ✅ memory 工具批量操作（`operations` 数组）

**设计依据**: §5.1 | **规模**: ~1 天 | **依赖**: 无 | **评审**: ✅ 批准
**子任务**:
- [x] HP-101a：schema 增加 `operations[]`，与顶层 `action` 互斥
- [x] HP-101b：`MemoryStore.apply_batch()` — 锁顺序 `memory`→`user`，内存 simulate 后一次写盘
- [x] HP-101c：任一条失败整批回滚；返回逐条 `results` + `summary`
- [x] HP-101d：`tool_executor.py` 批量 add/replace 桥接 `on_memory_write()`（remove 仍不桥接）
- [x] HP-101e：测试 — 成功 / 失败回滚 / drift / 互斥 / 跨 target
**验收**: replace/remove 无匹配时整批失败且文件未变；单 action 路径零回归；`MemoryProvider` ABC 零改动。

### [HP-102] ✅ Gateway 按通道模型覆盖

**设计依据**: §5.2 + 评审修订 4 | **规模**: ~2 天 | **依赖**: 无 | **评审**: ✅ 批准
**键规范**（见细化文档 §3.2）：`gateway.model_overrides` 支持平台名（`telegram`）与会话后缀键（`telegram:dm:12345`）；查找在 session `/model` 之前。
**子任务**:
- [x] HP-102a：`DEFAULT_CONFIG["gateway"]["model_overrides"]`
- [x] HP-102b：`gateway/config.py` bridge（via raw YAML → `_load_gateway_config`）
- [x] HP-102c：`config_helpers._resolve_config_model_override(session_key, config)`
- [x] HP-102d：`agent_runner._resolve_session_agent_runtime()` 合并 config override
- [x] HP-102e：三条 config loader 各 1 测试
- [x] HP-102f：优先级矩阵（session > channel > platform > global）
- [ ] HP-102g：（可选）doctor 未知 platform 名校验
**验收**: 无 override 时与 main 行为一致；三条 loader 测试通过。

### [HP-103] 🔄 图像编辑（源图 → provider 编辑端点）

**设计依据**: §5.8 + 评审修订 5 | **规模**: MVP ~2d + FAL ~1–2d（可溢出）| **依赖**: 无 | **评审**: ⚠️ 分期批准
**Phase 1 MVP（HP-103a–e）**:
- [x] HP-103a：ABC `supports_edit` + 默认 `edit()` 能力错误
- [x] HP-103b：tool `source_image?` + `is_forbidden_path` + 文件存在性
- [x] HP-103c：有源图且 supports_edit → `edit()`，否则明确报错（不 silent generate）
- [x] HP-103d：OpenAI plugin `images.edit` + cache 输出
- [x] HP-103e：安全测试（forbidden path、unsupported provider）
**溢出 / Phase 2 首项（HP-103f）**:
- [ ] HP-103f：FAL 编辑端点 + catalog 扩展
**明确不做（Phase 1）**: URL 源图、mask/inpaint 高级参数。
**验收**: 无 `source_image` 时与 main 一致；敏感路径被拒。

---

## Phase 2 — P1 上（后台委派 + /learn）

> **细化与评审**：`2026-07-08-hermes-phase2-refinement-review.md` | **Spike**：`2026-07-08-hermes-hp201-spike-conclusion.md`
> **状态**：🔄 MVP 已实现（未 commit）；HP-103f 仍可选

### [HP-103f] ⬜ FAL 图像编辑（Phase 1 溢出，可选）

**设计依据**: §5.8 | **规模**: 1–2 天 | **依赖**: HP-103 MVP | **评审**: ⚠️ 可选
- [ ] HP-103f-a：FAL edit API spike
- [ ] HP-103f-b：FAL plugin `edit()` + catalog
- [ ] HP-103f-c：复用 `validate_local_image_file()`
- [ ] HP-103f-d：测试（mock + forbidden path）

### [HP-201] ✅ [SPIKE] CLI 完成回流验证（HP-202 硬门禁）

**设计依据**: 评审修订 2 + Phase 2 细化 §4 | **规模**: ~1 天
**子任务**:
- [x] HP-201a–d：结论文档 `2026-07-08-hermes-hp201-spike-conclusion.md`
**验收**: gateway 走 `internal=True`；fan-out 单回合合并；CLI 自动 drain。

### [HP-202] 🔄 后台委派注册表（Rust）+ `delegate_task(background=true)`

**设计依据**: §5.3 + Phase 2 细化 §5 | **规模**: ~1.5–2 周 | **依赖**: HP-201
**P1 评审修复（2026-07-08）**: drain 截断丢失、c cancel 语义、`.so` 导入链 — 见 `phase2-quality-security-review.md`
**子任务**:
- [x] HP-202a：`rust-core/src/delegation.rs`
- [x] HP-202b：`intellect_rust.py` 导出（需 `maturin develop`）
- [x] HP-202c：`tools/async_delegation.py`
- [x] HP-202d：`delegate_tool.py` `background: bool` + 立即返回 handle
- [x] HP-202e：sync + bg 共享 `max_concurrent_children`
- [x] HP-202f：Gateway `_run_delegation_watcher` + `internal=True`
- [x] HP-202g：CLI `drain_completion_notifications` in `_process_loop`
- [x] HP-202h：父 `/stop` 不级联（background 线程独立）
- [x] HP-202j：P1/P2 评审修复 — `drain_completions_up_to`、cancel interrupt、requeue、注入 framing、`intellect_rust` 导入
- [x] HP-202i / HP-205a：E2E `tests/gateway/test_background_delegation.py`（11 tests）
**验收**: `background=false` 零回归；`maturin develop --release` 后 Rust registry 可用。

### [HP-203] ✅ `/delegations` slash 命令（原设计 `/bg` 已改名）

**设计依据**: §5.3 + Phase 2 细化 §6 | **规模**: ~1 天 | **依赖**: HP-202
- [x] HP-203a–f：`CommandDef` + CLI/gateway handlers + 测试
- [x] HP-203e：`display.background_process_notifications` 分级（delegation watcher）
**验收**: `/bg` 仍 → `/background`；`/delegations list|show|cancel` 可用。

### [HP-204] 🔄 `/learn` 技能蒸馏

**设计依据**: §5.5 + Phase 2 细化 §7 | **规模**: ~3–4 天 | **依赖**: 无
**子任务**:
- [x] HP-204a：`auxiliary.learn` in `DEFAULT_CONFIG`
- [x] HP-204b：`agent/learn_prompt.py`
- [x] HP-204c：`learn_command` provenance
- [x] HP-204d–f：CLI + gateway `/learn` / `save` / `discard`
- [x] HP-204g：集成测试（mock LLM + mock skill_manage save）
- [x] HP-204i：隐私提示（CommandDef + generate 输出）
- [x] HP-204h：（可选）`validate_skill_frontmatter_rs`（→ HP-205c ✅）
**验收**: confirm-before-write；拒绝不落盘。

---

## Phase 2.5 — P2/P3 收尾（评审跟进，可并行 Phase 3）

> **来源**：`2026-07-08-hermes-phase2-quality-security-review.md`  
> **原则**：不阻塞 Phase 3 spike；E2E 与 Rust 校验可独立 PR。

### [HP-205] ✅ Phase 2 质量收尾包

| ID | 子任务 | 产出 | 估时 | 优先级 |
|----|--------|------|------|--------|
| HP-205a | ✅ Gateway E2E：background 完成 → `internal=True` 注入 → 父回合 | `tests/gateway/test_background_delegation.py` (11 tests) | 4–6h | P2 |
| HP-205b | ✅ CI smoke：`HAS_DELEGATION_REGISTRY` + `drain_completions_up_to` 集成 | `tests/test_intellect_rust_delegation.py` | 2h | P2 |
| HP-205c | ✅ Rust `validate_skill_frontmatter_rs`（HP-204h 溢出） | `rust-core` + `skill_manager_tool._validate_frontmatter` | 4h | P3 |
| HP-205d | ✅ `/learn` 可选 `auxiliary.learn.redact_secrets`（复用 `agent.redact`） | config + `build_learn_messages` | 3h | P3 |
| HP-205e | ✅ maturin/post-install 脚本：同步 python-source `.so` | `scripts/sync_rust_extension.sh` | 1h | P3 |
| HP-205f | ✅ Gateway 重复 watcher inject 回归（post-turn 去重 E2E） | 扩展 HP-205a (12 tests) | 2h | P3 |

**验收**: HP-205a/b 合并前建议完成；c–f 可随 Phase 3 并行。

---

## Phase 3 — P1 下（验证证据 + MoA + 蓝图）

> **细化日期**：2026-07-08（评审后第二轮）  
> **并行建议**：HP-301 spike ∥ HP-303 台账 ∥ HP-304 蓝图 catalog（待 Q7）  
> **硬门禁**：HP-302 必须在 HP-301 评审签字后开工

### [HP-301] ⬜ [SPIKE] MoA 拦截点选型短文档（HP-302 前置）

**设计依据**: 评审修订 1（风险升级为高）| **规模**: ~1 天 | **依赖**: 无
**子任务**:
| ID | 内容 | 产出 |
|----|------|------|
| HP-301a | 梳理现有 `mixture_of_agents` 工具 vs 虚拟 provider 路径 | 对比表 |
| HP-301b | 评估拦截点 A：`agent/agent_init.py` client 工厂 | 时序图 + 风险 |
| HP-301c | 评估拦截点 B：`chat_completion_helpers.py` 请求入口 | 时序图 + 风险 |
| HP-301d | 流式 / fallback_model / smart_model_routing 影响矩阵 | 1 页 |
| HP-301e | 评审签字 + HP-302 实施顺序 | spike 文档定稿 |
**验收**: 文档 ≤5 页；明确「带 tool call 的回合检测到 `moa/` 如何报错」。

### [HP-302] ⬜ MoA 虚拟 provider（`moa/<preset>`）

**设计依据**: §5.6 | **规模**: ~1 周 | **依赖**: HP-301 ✅
**子任务（按实施顺序）**:
| ID | 内容 | 文件 |
|----|------|------|
| HP-302a | ProviderProfile 插件 + preset 配置 schema | `plugins/model-providers/moa/`, `intellect_cli/moa_config.py` |
| HP-302b | 参考模型并行 + 聚合器 loop | `agent/moa_loop.py` |
| HP-302c | 分层 trace 写会话 DB | `agent/moa_trace.py` |
| HP-302d | HP-301 选定拦截点接线 | 按 spike 结论 |
| HP-302e | Token 用量 → Rust `TokenAccumulator`；MoA 豁免二级路由 | `run_agent.py` 薄挂钩 |
| HP-302f | CommandDef `/moa` + legacy 工具文档指向 | `commands/registry.py` |
| HP-302g | 成本明示 UI（调用前 N+1 提示） | CLI + gateway help |
| HP-302h | 测试：无工具纯文本 E2E；带工具回合报错 | `tests/agent/test_moa_*.py` |
**验收**: `moa/<preset>` 可被 `/model`、cron、auxiliary 选中；trace WebUI 可见；OpenRouter 硬绑定解除。

### [HP-303] ⬜ 验证证据台账（Rust）+ verify-on-stop + `/goal` 契约

**设计依据**: §5.4 + 评审修订 3 | **规模**: ~1–1.5 周 | **依赖**: 无
**子任务（分三波）**:
**波 1 — 存储与分类（3–4d）**
| ID | 内容 |
|----|------|
| HP-303a | `rust-core/src/verification.rs` + schema bump（协调 `intellect_state.py` v15→v16） |
| HP-303b | 验证命令分类正则（pytest/cargo test/npm test/go test/make check…） |
| HP-303c | `agent/verification_evidence.py` 薄封装 + fail-open 单测 |
**波 2 — 写入挂钩（2–3d）**
| ID | 内容 |
|----|------|
| HP-303d | terminal 验证类命令完成 → 写证据 |
| HP-303e | `rust_file_mutation_landed` 通过 → 写证据 |
| HP-303f | 配置门控 `agent.verification.enabled`（**默认 false**） |
**波 3 — 消费面（2–3d）**
| ID | 内容 |
|----|------|
| HP-303g | `agent/verification_stop.py` + `conversation_loop.py` 两挂钩 |
| HP-303h | `intellect_cli/goals.py` judge 注入证据；`/goal` 完成契约语法 |
| HP-303i | `webui/api/goals.py` 证据列表（**前置 Q6 WebUI 带宽**） |
**验收**: 台账故障不阻塞回合；关闭门控时零行为变化；`cargo test` 分类覆盖。

### [HP-304] ⬜ Automation Blueprints

**设计依据**: §5.7 | **规模**: ~1 周 | **依赖**: 无 | **启动前置**: Q7 蓝图清单
**子任务**:
| ID | 内容 | 估时 |
|----|------|------|
| HP-304a | `rust-core/src/blueprints.rs` YAML 解析 + 参数 schema | 1d |
| HP-304b | 双源 catalog（内置 + `{INTELLECT_HOME}/blueprints/`） | 0.5d |
| HP-304c | `tools/blueprints.py` → `cronjob` 工具集 | 1d |
| HP-304d | `cron/jobs.py` 增加 `blueprint_id` 溯源 | 0.5d |
| HP-304e | CommandDef `/blueprint` + CLI | 1d |
| HP-304f | SKILL frontmatter `metadata.intellect.blueprint` 注册 | 0.5d |
| HP-304g | 内置 5–10 蓝图（**待 Q7 产品输入**） | 1–2d |
| HP-304h | 非法参数 Rust 层拒测 + instantiate E2E | 0.5d |
**验收**: 实例化 cron job 可 `intellect cron list` 溯源；非法参数可读错误。

### Phase 3 出口检查清单

- [ ] HP-301 spike 签字归档
- [ ] HP-302 + HP-303 各自 `scripts/run_tests.sh` 通过
- [ ] HP-304 至少 1 个内置蓝图 E2E（Q7 到位后）
- [ ] Phase 2.5 HP-205a E2E 通过（可与 Phase 3 并行，不阻塞 301 spike）

---

## Phase 4 — P2（依 Phase 2/3 反馈裁剪）

### [HP-401] ⬜ `/journey` 学习图谱

**设计依据**: §5.10 | **依赖**: HP-303（证据事件源）、HP-204（技能事件源）
- [ ] `learning_graph` 事件表 + 边表（Rust 落地，schema bump 协调同 HP-303）
- [ ] 事件源接入：skill provenance / memory 写入 / goal 判定 / 验证证据（不新增采集面）
- [ ] 时间线聚合 CTE（Rust，模式对齐 `compression.rs`）
- [ ] CommandDef `/journey`（CLI 时间线输出）
- [ ] WebUI 图谱页（**启动前置**：设计 §八 Q6 WebUI 带宽确认）

### [HP-402] ⬜ SessionDB 写合并队列（Rust，替代 AsyncSessionDB）

**设计依据**: §5.9.1 | **依赖**: 无（但建议在 HP-202/303 的 Rust 改动稳定后进行）
- [ ] `backend.rs` 读路径 GIL 释放审计
- [ ] 写合并队列（批量 flush 消息；同时解决 optimization-roadmap P9）
- [ ] 崩溃一致性测试（进程 kill 后 WAL 恢复）
- [ ] **不引入** Python asyncio DB 层

### [HP-403] ⬜ WhatsApp Cloud API 适配器

**设计依据**: §5.11 — `plugins/platforms/whatsapp_cloud/`（新），与 web bridge 并存；
token 锁按 `acquire_scoped_lock()` 模式。

### [HP-404] ⬜ 1Password secret source

**设计依据**: §5.11 — `agent/secret_sources/onepassword.py`，走 `op` CLI，对齐 Bitwarden 实现模式。

### [HP-405] ⬜ Google Vertex AI provider

**设计依据**: §5.11 | **启动前置**: 设计 §八 Q5（国内可达性/用户分布确认）
- [ ] `plugins/model-providers/vertex/` + `agent/vertex_adapter.py`（OAuth2 复用 `agent/oauth/` + Rust crypto）

### [HP-406] ⬜ Gateway scale-to-zero

**设计依据**: §5.9.2 | **启动前置**: 设计 §八 Q4（部署形态需求确认，无需求则 icebox）
- [ ] 空闲超时自停 + systemd socket activation；长轮询平台配置校验拒绝
- [ ] 门控 `gateway.scale_to_zero.enabled`（默认 false）

---

## 跳过 / 暂缓（决议记录，不再追踪）

| 项 | 决议 |
|----|------|
| Photon iMessage | ❌ 跳过 — BlueBubbles 已覆盖 iMessage |
| Raft 代理网络 | ⏸️ 暂缓 — 与 kanban/SimpleX 定位重叠，待上游生态成熟 |
| Desktop 专属功能（Projects UI、Star Map、原生通知等） | ❌ 不移植 — 无 Desktop 客户端；可视化由 WebUI 承接（HP-401） |
| Pets | ⏸️ 可选 icebox — 士气项目，不排期 |
| Chronos 托管 cron | ⏸️ 随 HP-406 决议 |
| Gateway relay 连接器 | ⏸️ 仅需求调研，不实施 |

---

## 依赖关系图

```text
HP-001/002 (Phase 0, 独立)

HP-101 ─┐
HP-102 ─┼─ (Phase 1, 互相独立可并行)
HP-103 ─┘

HP-103f (可选) ── Phase 1 FAL 溢出

HP-201 (spike) ──► HP-202 ──► HP-203 (/delegations，非 /bg)
HP-204 (独立, 可与 HP-202 并行，201 后)
HP-205 (Phase 2.5 收尾, 可与 Phase 3 并行)

HP-301 (spike) ──► HP-302
HP-303 (独立) ──► HP-401 (事件源之一)
HP-304 (独立, 待 Q7 蓝图清单)
HP-204 ──────────► HP-401 (事件源之一)

HP-402..406 (Phase 4, 各自独立, 部分有启动前置问题)
```

## 未决问题绑定（来自设计 §八，评审后仍开放）

| 问题 | 绑定任务 | 性质 |
|------|----------|------|
| Q4 scale-to-zero 需求 | HP-406 | 启动前置，未确认则 icebox |
| Q5 Vertex 优先级 | HP-405 | 启动前置 |
| Q6 WebUI 带宽 | HP-401（图谱页部分） | 启动前置 |
| Q7 首发蓝图清单 | HP-304 | 启动前置，需产品输入 |

已定案：Q1（/stop 不级联）、Q2（verification 默认 false 灰度一个 minor）、
Q3（MoA 首版仅成本明示）——见设计文档评审记录 §9.2。
