{
  "name": "intellect-vault-quartz",
  "private": true,
  "description": "Quartz build workspace for Intellect Vault",
  "scripts": {
    "setup": "git clone --depth 1 --branch v4 https://github.com/jackyzha0/quartz.git _quartz && cd _quartz && npm install",
    "build": "cd _quartz && npx quartz build"
  }
}
